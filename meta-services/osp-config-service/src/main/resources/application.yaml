spring:
  application:
    name: config-service
  profiles:
    active:
      - default
      - native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
  output.ansi.enabled: always
server:
  port: 80

logging:
  level:
    root: info

management:
  endpoint:
    health:
      show-details: always
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: '*'

---
spring:
  config:
    activate.on-profile: default
server:
  port: 8888